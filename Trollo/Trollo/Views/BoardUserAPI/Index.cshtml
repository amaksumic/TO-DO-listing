@model IEnumerable<Trollo.boardmembers>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idboardmembers)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idkorisnik)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idploca)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idboardmembers)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idkorisnik)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idploca)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
